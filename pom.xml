<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>dansplugins</groupId>
  <artifactId>Medieval-Roleplay-Engine</artifactId>
  <version>2.0.0-SNAPSHOT-8-8-2026</version>
  <packaging>jar</packaging>

  <name>Medieval Roleplay Engine</name>

  <!--
    PATRIAM FORK. The default build targets the Minecraft the server actually runs, which is the
    whole reason this diverges from upstream. Upstream compiles against spigot-api 1.13.1: that
    still produces a loadable jar, but it cannot see a method 26.2 removed, so the build goes green
    and the failure surfaces as a NoSuchMethodError at the first call site a player reaches.
    Compiling against the deployed version is what turns that into a build error.

    Every source file compiles clean against 26.2 as of 283dfca, so this is a retarget, not a port.
    tools/build-all.sh asserts the resolved coordinate below matches its EXPECT_PAPER; bump both
    together or the build fails loudly, which is the intent.
  -->
  <properties>
    <paper.version>26.2.build.92-stable</paper.version>
    <java.version>25</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <dependency>
      <groupId>io.papermc.paper</groupId>
      <artifactId>paper-api</artifactId>
      <version>${paper.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>me.clip</groupId>
      <artifactId>placeholderapi</artifactId>
      <!-- Pinned. Upstream carries LATEST, which Maven deprecates and which re-resolves on every
           run: the version that compiled the staged jar is then whatever the remote held that
           minute, and nothing records it. 2.12.3 is what LATEST resolved to on 2026-08-15. -->
      <version>2.12.3</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>com.github.Dans-Plugins</groupId>
      <artifactId>Mailboxes</artifactId>
      <version>v1.1</version>
      <scope>compile</scope>
    </dependency>
    <dependency>
      <groupId>org.jetbrains</groupId>
      <artifactId>annotations</artifactId>
      <version>23.0.0</version>
      <scope>provided</scope>
    </dependency>

  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.8.1</version>
        <configuration>
          <!-- paper-api 26.x is Java 25 bytecode, so `release` rather than source/target: it
               pins the platform API too, instead of only the language level. -->
          <release>${java.version}</release>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.2.2</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <createDependencyReducedPom>false</createDependencyReducedPom>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>true</filtering>
      </resource>
    </resources>
  </build>

  <repositories>
    <repository>
      <id>papermc</id>
      <url>https://repo.papermc.io/repository/maven-public/</url>
    </repository>
    <repository>
      <id>sonatype</id>
      <url>https://oss.sonatype.org/content/groups/public/</url>
    </repository>
    <repository>
      <id>placeholderapi</id>
      <url>https://repo.extendedclip.com/content/repositories/placeholderapi/</url>
    </repository>
    <repository>
      <id>jitpack.io</id>
      <url>https://jitpack.io</url>
    </repository>
  </repositories>
</project>
